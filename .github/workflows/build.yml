name: Cross-Platform Build and Test

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
    - uses: actions/checkout@v4

    - name: Build C Library
      run: |
        cmake -S core -B core/build -DCMAKE_BUILD_TYPE=Release
        cmake --build core/build --config Release

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        
    - name: Test Python Wrapper
      shell: bash
      run: |
        # Try finding the DLL in common places
        if [ -f "core/build/Release/pqc.dll" ]; then
          cp "core/build/Release/pqc.dll" wrappers/python/src/pqc_sdk/
        elif [ -f "core/build/pqc.dll" ]; then
          cp "core/build/pqc.dll" wrappers/python/src/pqc_sdk/
        elif [ -f "core/build/libpqc.so" ]; then
          cp "core/build/libpqc.so" wrappers/python/src/pqc_sdk/
        elif [ -f "core/build/libpqc.dylib" ]; then
          cp "core/build/libpqc.dylib" wrappers/python/src/pqc_sdk/
        fi
        
        cd wrappers/python
        # Use PYTHONPATH to ensure local import works
        export PYTHONPATH=$PYTHONPATH:.
        python -c "import src.pqc_sdk as pqc; p, s = pqc.generate_keypair(); ct = pqc.encrypt_bit(1, p); res = pqc.decrypt_bit(ct, s); print(f'Decrypted: {res}'); assert res == 1"

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Test Node Wrapper
      shell: bash
      run: |
        if [ -f "core/build/Release/pqc.dll" ]; then
          cp "core/build/Release/pqc.dll" wrappers/js/
        elif [ -f "core/build/pqc.dll" ]; then
          cp "core/build/pqc.dll" wrappers/js/
        elif [ -f "core/build/libpqc.so" ]; then
          cp "core/build/libpqc.so" wrappers/js/
        elif [ -f "core/build/libpqc.dylib" ]; then
          cp "core/build/libpqc.dylib" wrappers/js/
        fi
        
        cd wrappers/js
        npm install
        npm test

    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '11'

    - name: Test Java Wrapper
      shell: bash
      run: |
        if [ -f "core/build/Release/pqc.dll" ]; then
          LIB_PATH="$(pwd)/core/build/Release"
        else
          LIB_PATH="$(pwd)/core/build"
        fi
        cd wrappers/java
        # On Windows, convert to backslashes for JNA if necessary, but absolute path is usually enough
        mvn clean compile
        mvn exec:java -Dexec.mainClass="com.pqc.QuantumSafeSDK" -Djna.library.path="$LIB_PATH"

    - name: Set up PHP
      if: matrix.os != 'windows-latest'
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.1'
        extensions: ffi

    - name: Test PHP Wrapper
      if: matrix.os != 'windows-latest'
      shell: bash
      run: |
        if [ -f "core/build/libpqc.dylib" ]; then
          LIB_FILE="$(pwd)/core/build/libpqc.dylib"
        else
          LIB_FILE="$(pwd)/core/build/libpqc.so"
        fi
        cd wrappers/php
        echo "<?php require 'PqcClient.php'; \$client = new Pqc\PqcClient('$LIB_FILE'); \$kp = \$client->generateKeyPair(); \$ct = \$client->encryptBit(1, \$kp['pub']); \$res = \$client->decryptBit(\$ct, \$kp['sec']); echo 'Decrypted: ' . \$res . PHP_EOL; if(\$res !== 1) exit(1);" > test.php
        php -d ffi.enable=1 test.php

    - name: Upload Build Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: qingabo-libs-${{ matrix.os }}
        path: |
          core/build/libpqc.so
          core/build/libpqc.dylib
          core/build/Release/pqc.dll
          core/build/pqc.dll
        if-no-files-found: ignore
